#!/usr/bin/env python
# -*- coding: utf8 -*-

import sys
import bundler

if len(sys.argv) < 2:
    print "[Warning]I need of folder as argument"
    print "$ bundler images/"
else:
    bundler.bundle(sys.argv[1])
